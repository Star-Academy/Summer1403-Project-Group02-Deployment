{{- if .Values.api.pullSecret.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.api.pullSecret.name }}
data:
  {{ .Values.api.pullSecret.data | toYaml | nindent 2 }}
type: kubernetes.io/dockerconfigjson
{{- end -}}
